# OpenAI API key for article generation
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Optional: model to use (default: gpt-4o-mini)
# AI_MODEL=gpt-4o-mini

# Optional: number of articles to generate per run (0 = all, default: 0)
# BATCH_SIZE=5
